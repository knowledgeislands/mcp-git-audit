# Colon-separated list of absolute (or ~/...) paths that the audit tool is
# allowed to walk. Any `root` argument must be equal to or inside one of these
# after ~ expansion and realpath normalisation. Defaults to "~" (the user's
# home directory) when unset or empty.
MCP_GIT_AUDIT_SAFE_ROOTS=~/dev
